
# Core framework
flask
flask-sqlalchemy
flask-login
flask-apscheduler

# Auth / validation
werkzeug
email-validator

# Database driver — psycopg2-binary enables PostgreSQL (required for Vercel + Neon)
psycopg2-binary

# Production WSGI server
gunicorn
